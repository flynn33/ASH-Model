# Roadmap 008 Validation Outputs

Generated by:

```bash
python tools/generate_branch_measure.py --out-root .
```

The output `verification.json` must report:

- `normalization_check.passed == true`
- total frontier measure residual at or below `1e-12`
- total amplitude-norm residual at or below `1e-12`
- local sibling-probability residual at or below `1e-12`

These checks verify only finite normalization and deterministic artifact
generation. They do not validate a physical branch ontology, a Born rule,
decoherence, observer commitment, or empirical cosmology.
